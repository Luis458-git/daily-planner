import { DAYS } from '../Services/routineService'
export default function RoutineItem({ activity, date, onToggle, onEdit, onDelete, onFocus }) {
  const completed = activity.completedDates.includes(date)
  return <article className={`routine-item category-${activity.category} ${completed ? 'completed' : ''}`}><input type="checkbox" checked={completed} onChange={() => onToggle(activity.id, date)} aria-label={`Completar ${activity.title}`} /><div className="routine-content"><h3>{activity.title} <span className="badge">{activity.category}</span></h3><p>◷ {activity.start} – {activity.end} <span>· {activity.days.map(day => DAYS[day].slice(0, 3)).join(' · ')}</span></p><small>{completed ? 'Completada' : 'Pendiente'} · {activity.reminder === 'off' ? 'Sin recordatorio' : activity.reminder === '0' ? 'Aviso a la hora exacta' : `Aviso ${activity.reminder} min antes`}</small></div><div className="item-actions"><button onClick={() => onFocus(activity.id)} aria-label={`Enfocar ${activity.title}`}>▷</button><button onClick={() => onEdit(activity)} aria-label={`Editar ${activity.title}`}>✎</button><button onClick={() => onDelete(activity)} aria-label={`Eliminar ${activity.title}`}>×</button></div></article>
}
