import { DAYS } from '../Services/routineService'
const links = [['dashboard', '▦', 'Dashboard'], ['week', '▤', 'Rutina semanal'], ['timer', '◷', 'Temporizador'], ['activities', '✓', 'Actividades'], ['stats', '⌁', 'Estadísticas']]
export default function Sidebar({ page, navigate, open, percent }) {
  return <aside className={`sidebar ${open ? 'is-open' : ''}`}><a className="brand" href="#dashboard" onClick={() => navigate('dashboard')}><span className="brand-icon">ϟ</span><span>Flowy<small>DAILY PLANNER</small></span><span className="live-label">● Local</span></a><nav aria-label="Navegación principal">{links.map(([id, icon, label]) => <button key={id} className={page === id ? 'active' : ''} onClick={() => navigate(id)} aria-current={page === id ? 'page' : undefined}><span aria-hidden="true">{icon}</span>{label}</button>)}</nav><div className="sidebar-bottom"><p>✧ Tu ritmo, tus metas</p><progress value={percent} max="100" aria-label="Progreso semanal" /><small>{percent}% completado esta semana</small><p className="green">● Un paso a la vez</p><small>{DAYS.length} días para organizarte</small></div></aside>
}
